[package]
name = "plato-vision-jepa"
version = "0.1.0"
edition = "2021"
description = "Vision JEPA for PLATO nervous system — room perception from camera frames"
license = "Apache-2.0"
repository = "https://github.com/SuperInstance/plato-vision-jepa"

[dependencies]
serde = { version = "1", features = ["derive"] }
uuid = { version = "1", features = ["v4", "serde"] }
